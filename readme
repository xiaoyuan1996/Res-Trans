环境：python3, torch1.6,cv2
运行环境： CUDA10.0, TITAN RTX(我们只有这个卡)

1.读data文件夹下的readme。
2.读取ExtractImage下的readme，并执行。
3.读取DataArgument下的readme，按步骤执行。
4.运行Train下的文件，运行多个模型生成不同的概率json。
5.运行Ensemble下的文件，进行模型集成，得到最后的提交文件。

最快验证结果的方法：
1.运行Ensemble下的ensemble.py，将我们之前训练好的模型得分进行加和，得到submission.csv
2.数据都打包好了，因此也可直接运行Train下的main.py。通过config来改参数，main里边来选模型。
